Ce qu’un jury te demandera — prépare des réponses claires

Comment tu gères les conflits stock en mode offline-first ? (stratégie de synchronisation, résolution de conflits)

Comment sécurises-tu l’accès ? (USER table, bcrypt, JWT, rôles/permissions)

Comment calcules-tu la marge / coût de revient ? (méthode de valorisation, BOM cost aggregation)

Flux complet : Devis → Proforma → BL → Facture → Paiement — montre les triggers, états et références parent.

Test (unit/integration) : exemples de tests pour insertion de commande, déclenchement de mouvements stock, génération PDF.

Cas d’usage concurrent : 2 cassières enregistrent la même variante simultanément — que se passe-t-il ?

Gestion des retours et avoirs.

Plan de migration des prix historiques.

Logs / audit / GDPR si données personnelles clients.